Airports:
    LOAD
      rowID As id,
      REPLACE(Airport, 'Airport', '') as Name, //If a Airport name includes Airport remove it
      City,
      UPPER(Country) as Country
    FROM [lib://data/airports.csv]
    (txt, utf8, embedded labels, delimiter is ',');
