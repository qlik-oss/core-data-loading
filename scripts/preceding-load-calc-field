// The load script below shows how to use preceding LOAD to calculate a new
// field, Tax, in one table in a single pass.

Taxes:
    LOAD *, Salary * 0.10 as Tax;
    LOAD * FROM [lib://data/salaries.csv]
